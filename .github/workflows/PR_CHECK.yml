name: PR Check

on:
  pull_request:
    branches: [ "main" ]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Build Docker image
        run: docker build -t trend-app .

      - name: Run handler locally
        run: |
          python -c "from ingestion.fetch_data import collect_all; print('OK')"